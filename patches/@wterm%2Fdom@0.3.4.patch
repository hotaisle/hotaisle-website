diff --git a/dist/renderer.js b/dist/renderer.js
index 5d6c9c92fa0ab0684854e2e9113a4062608f088a..6b0e0e3cf077ee1bbf987a033c8f65a0d1f0709e 100644
--- a/dist/renderer.js
+++ b/dist/renderer.js
@@ -7,13 +7,43 @@ const FLAG_REVERSE = 0x20;
 const FLAG_INVISIBLE = 0x40;
 const FLAG_STRIKETHROUGH = 0x80;
 const DEFAULT_SCROLLBACK_OVERSCAN_ROWS = 10;
-function rgbToCSS(packed) {
+const LINK_PATTERN = /(?:(?:https?|ssh):\/\/[^\s<>"']*[^\s<>"'(),.;!?]|\bssh\s+(?:-p\s+\d+\s+)?[a-z0-9._-]+@[a-z0-9.-]+(?::\d+)?)/giu;
+const SSH_COMMAND_PATTERN = /^ssh\s+(?:-p\s+(\d+)\s+)?(\S+)$/u;
+const BOX_DRAWING_CLASSES = new Map([
+    [0x2500, "term-box-h"],
+    [0x2501, "term-box-h"],
+    [0x2502, "term-box-v"],
+    [0x2503, "term-box-v"],
+    [0x250c, "term-box-tl"],
+    [0x250f, "term-box-tl"],
+    [0x2510, "term-box-tr"],
+    [0x2513, "term-box-tr"],
+    [0x2514, "term-box-bl"],
+    [0x2517, "term-box-bl"],
+    [0x2518, "term-box-br"],
+    [0x251b, "term-box-br"],
+    [0x2550, "term-box-h"],
+    [0x2551, "term-box-v"],
+    [0x2554, "term-box-tl"],
+    [0x2557, "term-box-tr"],
+    [0x255a, "term-box-bl"],
+    [0x255d, "term-box-br"],
+    [0x256d, "term-box-tl"],
+    [0x256e, "term-box-tr"],
+    [0x256f, "term-box-br"],
+    [0x2570, "term-box-bl"],
+]);
+function adjustDirectForeground(color) {
+    return `color-mix(in srgb,${color} var(--term-direct-fg-weight,100%),var(--term-direct-fg-mix,transparent))`;
+}
+function rgbToCSS(packed, adjustForeground = false) {
     const r = (packed >> 16) & 0xff;
     const g = (packed >> 8) & 0xff;
     const b = packed & 0xff;
-    return `rgb(${r},${g},${b})`;
+    const color = `rgb(${r},${g},${b})`;
+    return adjustForeground ? adjustDirectForeground(color) : color;
 }
-function colorToCSS(index) {
+function colorToCSS(index, adjustForeground = false) {
     if (index === DEFAULT_COLOR)
         return null;
     if (index < 16)
@@ -23,15 +53,17 @@ function colorToCSS(index) {
         const r = Math.floor(n / 36) * 51;
         const g = (Math.floor(n / 6) % 6) * 51;
         const b = (n % 6) * 51;
-        return `rgb(${r},${g},${b})`;
+        const color = `rgb(${r},${g},${b})`;
+        return adjustForeground ? adjustDirectForeground(color) : color;
     }
     const level = (index - 232) * 10 + 8;
-    return `rgb(${level},${level},${level})`;
+    const color = `rgb(${level},${level},${level})`;
+    return adjustForeground ? adjustDirectForeground(color) : color;
 }
-function cellFgCSS(fg, fgRgb) {
+function cellFgCSS(fg, fgRgb, adjustForeground = false) {
     if (fgRgb !== undefined)
-        return rgbToCSS(fgRgb);
-    return colorToCSS(fg);
+        return rgbToCSS(fgRgb, adjustForeground);
+    return colorToCSS(fg, adjustForeground);
 }
 function cellBgCSS(bg, bgRgb) {
     if (bgRgb !== undefined)
@@ -52,7 +84,8 @@ function buildCellStyle(fg, bg, flags, fgRgb, bgRgb) {
         if (bgR === undefined && bgIdx === DEFAULT_COLOR)
             bgIdx = 7;
     }
-    const fgCSS = cellFgCSS(fgIdx, fgR);
+    const backgroundIsDefault = bgR === undefined && bgIdx === DEFAULT_COLOR;
+    const fgCSS = cellFgCSS(fgIdx, fgR, backgroundIsDefault);
     const bgCSS = cellBgCSS(bgIdx, bgR);
     let style = "";
     if (fgCSS)
@@ -62,7 +95,7 @@ function buildCellStyle(fg, bg, flags, fgRgb, bgRgb) {
     if (flags & FLAG_BOLD)
         style += "font-weight:bold;";
     if (flags & FLAG_DIM)
-        style += "opacity:0.5;";
+        style += "opacity:var(--term-dim-opacity,0.5);";
     if (flags & FLAG_ITALIC)
         style += "font-style:italic;";
     const decorations = [];
@@ -90,12 +123,29 @@ function escapeHTML(text) {
         .replace(/>/g, "&gt;")
         .replace(/"/g, "&quot;");
 }
+function linkifyText(text) {
+    let html = "";
+    let lastIndex = 0;
+    for (const match of text.matchAll(LINK_PATTERN)) {
+        const matchIndex = match.index ?? 0;
+        const label = match[0];
+        const sshCommand = label.match(SSH_COMMAND_PATTERN);
+        const href = sshCommand
+            ? `ssh://${sshCommand[2]}${sshCommand[1] ? `:${sshCommand[1]}` : ""}`
+            : label;
+        html += escapeHTML(text.slice(lastIndex, matchIndex));
+        html += `<a class="term-link" href="${escapeHTML(href)}" target="_blank" rel="noopener noreferrer">${escapeHTML(label)}</a>`;
+        lastIndex = matchIndex + label.length;
+    }
+    html += escapeHTML(text.slice(lastIndex));
+    return html;
+}
 function safeLinkHref(uri) {
     if (!uri)
         return undefined;
     try {
         const url = new URL(uri);
-        return url.protocol === "http:" || url.protocol === "https:"
+        return url.protocol === "http:" || url.protocol === "https:" || url.protocol === "ssh:"
             ? url.href
             : undefined;
     }
@@ -119,7 +169,7 @@ function resolveColors(fg, bg, flags, fgRgb, bgRgb) {
             bgIdx = 7;
     }
     return {
-        fg: cellFgCSS(fgIdx, fgR) || "var(--term-fg)",
+        fg: cellFgCSS(fgIdx, fgR, bgR === undefined && bgIdx === DEFAULT_COLOR) || "var(--term-fg)",
         bg: cellBgCSS(bgIdx, bgR) || "var(--term-bg)",
     };
 }
@@ -219,8 +269,6 @@ export class Renderer {
         this.rowEls = [];
         this.prevCursorRow = -1;
         this.prevCursorCol = -1;
-        this.prevContainerBg = "";
-        this.prevRowBg = [];
         this._scrollbackRowEls = [];
         this._scrollbackStartKey = 0;
         this._renderedScrollbackCount = -1;
@@ -234,7 +282,6 @@ export class Renderer {
         this.rows = rows;
         this.container.innerHTML = "";
         this.rowEls = [];
-        this.prevRowBg = [];
         this._scrollbackRowEls = [];
         this._scrollbackStartKey = 0;
         this._renderedScrollbackCount = -1;
@@ -256,7 +303,7 @@ export class Renderer {
         this.prevCursorRow = -1;
         this.prevCursorCol = -1;
     }
-    _buildRowContent(rowEl, getCell, lineLen, cursorCol, rowIndex) {
+    _buildRowContent(rowEl, getCell, lineLen, cursorCol) {
         let html = "";
         let runStyle = "";
         let runText = "";
@@ -265,6 +312,7 @@ export class Renderer {
         let runLinkKey = "";
         let runLinkUri;
         let outputLinkKey = "";
+        const toWidthStyle = (cellCount) => `width:calc(${cellCount} * var(--term-cell-width, 1ch));`;
         const appendContent = (content, linkKey, uri) => {
             const href = safeLinkHref(uri);
             const nextLinkKey = href ? linkKey : "";
@@ -281,7 +329,7 @@ export class Renderer {
         const flushRun = (endCol) => {
             if (!runText)
                 return;
-            const escaped = escapeHTML(runText);
+            const renderText = runLinkKey ? escapeHTML : linkifyText;
             let content = "";
             if (cursorCol >= runStart && cursorCol < endCol) {
                 const offset = cursorCol - runStart;
@@ -289,32 +337,25 @@ export class Renderer {
                 const cursorChar = runCells[offset] || " ";
                 const after = runCells.slice(offset + 1).join("");
                 if (before) {
-                    content += runStyle
-                        ? `<span style="${runStyle}">${escapeHTML(before)}</span>`
-                        : `<span>${escapeHTML(before)}</span>`;
+                    content += `<span style="${runStyle}${toWidthStyle(offset)}">${renderText(before)}</span>`;
                 }
-                content += runStyle
-                    ? `<span class="term-cursor" style="${runStyle}">${escapeHTML(cursorChar)}</span>`
-                    : `<span class="term-cursor">${escapeHTML(cursorChar)}</span>`;
+                content += `<span class="term-cursor" style="${runStyle}${toWidthStyle(1)}">${renderText(cursorChar)}</span>`;
                 if (after) {
-                    content += runStyle
-                        ? `<span style="${runStyle}">${escapeHTML(after)}</span>`
-                        : `<span>${escapeHTML(after)}</span>`;
+                    content += `<span style="${runStyle}${toWidthStyle(endCol - cursorCol - 1)}">${renderText(after)}</span>`;
                 }
             }
             else {
-                content += runStyle
-                    ? `<span style="${runStyle}">${escaped}</span>`
-                    : `<span>${escaped}</span>`;
+                content += `<span style="${runStyle}${toWidthStyle(endCol - runStart)}">${renderText(runText)}</span>`;
             }
             appendContent(content, runLinkKey, runLinkUri);
             runText = "";
             runCells = [];
         };
-        const appendStyledSpan = (className, style, text, linkKey, linkUri) => {
+        const appendStyledSpan = (className, style, text, linkKey, linkUri, cellCount) => {
             const classAttr = className ? ` class="${className}"` : "";
-            const styleAttr = style ? ` style="${style}"` : "";
-            appendContent(`<span${classAttr}${styleAttr}>${escapeHTML(text)}</span>`, linkKey, linkUri);
+            const styleAttr = ` style="${style}${toWidthStyle(cellCount)}"`;
+            const renderedText = linkKey ? escapeHTML(text) : linkifyText(text);
+            appendContent(`<span${classAttr}${styleAttr}>${renderedText}</span>`, linkKey, linkUri);
         };
         for (let col = 0; col < this.cols; col++) {
             const cell = getCell(col);
@@ -331,7 +372,7 @@ export class Renderer {
                 // would shorten the row.
                 const continuesWide = col > 0 && (getCell(col - 1).width ?? 1) === 2;
                 if (!continuesWide) {
-                    appendStyledSpan(col === cursorCol ? "term-cursor" : "", "", " ", cellLinkKey, cellLinkUri);
+                    appendStyledSpan(col === cursorCol ? "term-cursor" : "", "", " ", cellLinkKey, cellLinkUri, 1);
                 }
                 runStyle = "";
                 runLinkKey = "";
@@ -348,7 +389,7 @@ export class Renderer {
                 // continuation is outside the row. Drawing the pair here would spill
                 // a second column past the row.
                 if (col + 1 >= this.cols) {
-                    appendStyledSpan(col === cursorCol ? "term-cursor" : "", "", " ", cellLinkKey, cellLinkUri);
+                    appendStyledSpan(col === cursorCol ? "term-cursor" : "", "", " ", cellLinkKey, cellLinkUri, 1);
                     runStyle = "";
                     runLinkKey = "";
                     runLinkUri = undefined;
@@ -362,7 +403,7 @@ export class Renderer {
                 const cls = cursorCol >= col && cursorCol < col + 2
                     ? "term-wide term-cursor"
                     : "term-wide";
-                appendStyledSpan(cls, style, ch, cellLinkKey, cellLinkUri);
+                appendStyledSpan(cls, style, ch, cellLinkKey, cellLinkUri, 2);
                 runStyle = "";
                 runLinkKey = "";
                 runLinkUri = undefined;
@@ -371,13 +412,26 @@ export class Renderer {
                 runStart = col + 2;
                 continue;
             }
-            if (inBounds && cp >= 0x2580 && cp <= 0x259f) {
+            const boxDrawingClass = inBounds ? BOX_DRAWING_CLASSES.get(cp) : undefined;
+            if (boxDrawingClass) {
+                flushRun(col);
+                const style = buildCellStyle(cell.fg, cell.bg, cell.flags, cell.fgRgb, cell.bgRgb);
+                const cursorClass = col === cursorCol ? " term-cursor" : "";
+                appendContent(`<span class="term-box-drawing ${boxDrawingClass}${cursorClass}" style="${style}${toWidthStyle(1)}"></span>`, cellLinkKey, cellLinkUri);
+                runStyle = "";
+                runLinkKey = "";
+                runLinkUri = undefined;
+                runText = "";
+                runCells = [];
+                runStart = col + 1;
+            }
+            else if (inBounds && cp >= 0x2580 && cp <= 0x259f) {
                 flushRun(col);
                 const colors = resolveColors(cell.fg, cell.bg, cell.flags, cell.fgRgb, cell.bgRgb);
                 const cls = col === cursorCol ? "term-block term-cursor" : "term-block";
                 const bg = getBlockBackground(cp, colors.fg, colors.bg);
-                const dim = cell.flags & FLAG_DIM ? "opacity:0.5;" : "";
-                appendContent(`<span class="${cls}" style="background:${bg};${dim}"></span>`, cellLinkKey, cellLinkUri);
+                const dim = cell.flags & FLAG_DIM ? "opacity:var(--term-dim-opacity,0.5);" : "";
+                appendContent(`<span class="${cls}" style="background:${bg};${dim}${toWidthStyle(1)}"></span>`, cellLinkKey, cellLinkUri);
                 runStyle = "";
                 runLinkKey = "";
                 runLinkUri = undefined;
@@ -409,37 +463,12 @@ export class Renderer {
         if (outputLinkKey)
             html += "</a>";
         rowEl.innerHTML = html;
-        let bgCss = "";
-        if (lineLen >= this.cols && this.cols > 0) {
-            const lastCell = getCell(this.cols - 1);
-            let bgIdx = lastCell.bg;
-            let bgR = lastCell.bgRgb;
-            if (lastCell.flags & FLAG_REVERSE) {
-                bgIdx = lastCell.fg;
-                bgR = lastCell.fgRgb;
-                if (bgR === undefined && bgIdx === DEFAULT_COLOR)
-                    bgIdx = 7;
-            }
-            bgCss = cellBgCSS(bgIdx, bgR) || "";
-        }
-        const boxShadow = bgCss ? `0 1px 0 ${bgCss}` : "";
-        if (rowIndex >= 0) {
-            if (bgCss !== (this.prevRowBg[rowIndex] ?? "")) {
-                rowEl.style.background = bgCss;
-                rowEl.style.boxShadow = boxShadow;
-                this.prevRowBg[rowIndex] = bgCss;
-            }
-        }
-        else {
-            rowEl.style.background = bgCss;
-            rowEl.style.boxShadow = boxShadow;
-        }
     }
     _buildScrollbackRowEl(core, sbOffset) {
         const rowEl = document.createElement("div");
         rowEl.className = "term-row term-scrollback-row";
         const lineLen = core.getScrollbackLineLen(sbOffset);
-        this._buildRowContent(rowEl, (col) => core.getScrollbackCell(sbOffset, col), lineLen, -1, -1);
+        this._buildRowContent(rowEl, (col) => core.getScrollbackCell(sbOffset, col), lineLen, -1);
         return rowEl;
     }
     syncScrollback(core, viewport) {
@@ -530,43 +559,19 @@ export class Renderer {
     render(core, viewport) {
         const rows = core.getRows();
         const cols = core.getCols();
-        let resized = false;
         if (rows !== this.rows || cols !== this.cols) {
             this.setup(cols, rows);
-            resized = true;
         }
         this.syncScrollback(core, viewport);
         const cursor = core.getCursor();
         const cursorVisible = cursor.visible;
-        const needsCursorUpdate = cursor.row !== this.prevCursorRow || cursor.col !== this.prevCursorCol;
         for (let r = 0; r < this.rows; r++) {
-            const isDirty = resized || core.isDirtyRow(r);
-            const hadCursor = r === this.prevCursorRow && needsCursorUpdate;
             const hasCursor = r === cursor.row;
-            if (isDirty || hadCursor || (hasCursor && needsCursorUpdate)) {
-                const cCol = hasCursor && cursorVisible ? cursor.col : -1;
-                this._buildRowContent(this.rowEls[r], (col) => core.getCell(r, col), this.cols, cCol, r);
-            }
+            const cCol = hasCursor && cursorVisible ? cursor.col : -1;
+            this._buildRowContent(this.rowEls[r], (col) => core.getCell(r, col), this.cols, cCol);
         }
         this.prevCursorRow = cursor.row;
         this.prevCursorCol = cursor.col;
-        const lastRowDirty = resized || core.isDirtyRow(this.rows - 1);
-        if (lastRowDirty) {
-            const bottomRight = core.getCell(this.rows - 1, this.cols - 1);
-            let gridBgIdx = bottomRight.bg;
-            let gridBgRgb = bottomRight.bgRgb;
-            if (bottomRight.flags & FLAG_REVERSE) {
-                gridBgIdx = bottomRight.fg;
-                gridBgRgb = bottomRight.fgRgb;
-                if (gridBgRgb === undefined && gridBgIdx === DEFAULT_COLOR)
-                    gridBgIdx = 7;
-            }
-            const containerBg = cellBgCSS(gridBgIdx, gridBgRgb) || "";
-            if (containerBg !== this.prevContainerBg) {
-                this.container.style.background = containerBg;
-                this.prevContainerBg = containerBg;
-            }
-        }
         core.clearDirty();
     }
 }
diff --git a/dist/wterm.js b/dist/wterm.js
index b338e08f775cad6922df77db65803c3a1f0a3c76..12431b053b64cd384e56627909139489ee4ec5e1 100644
--- a/dist/wterm.js
+++ b/dist/wterm.js
@@ -4,6 +4,7 @@ import { InputHandler } from "./input.js";
 import { DebugAdapter } from "./debug.js";
 import { isLinkActivationModifier } from "./hyperlink.js";
 const SYNCHRONIZED_OUTPUT_TIMEOUT_MS = 1000;
+const INTERACTIVE_ELEMENT_SELECTOR = 'a[href], button, input, select, textarea, [role="button"]';
 export class WTerm {
     constructor(element, options = {}) {
         this.bridge = null;
@@ -41,13 +42,8 @@ export class WTerm {
             this.element.classList.add("cursor-blink");
         this._onClickFocus = (event) => {
             const target = event.target;
-            if (target instanceof Element && target.closest(".term-link")) {
-                if (isLinkActivationModifier(event, this.element.ownerDocument.defaultView?.navigator ?? navigator) ||
-                    event.detail === 0) {
-                    return;
-                }
-                event.preventDefault();
-            }
+            if (target instanceof Element && target.closest(INTERACTIVE_ELEMENT_SELECTOR))
+                return;
             const sel = window.getSelection();
             if (!sel || sel.isCollapsed)
                 this.input?.focus();
@@ -403,6 +399,7 @@ export class WTerm {
             return null;
         this._charWidth = charWidth;
         this._rowHeight = rowHeight;
+        this.element.style.setProperty("--term-cell-width", `${charWidth}px`);
         return { charWidth, rowHeight };
     }
     _setupResizeObserver() {
diff --git a/src/terminal.css b/src/terminal.css
index aed237b508f4d284b2e8d9fda0344f0766e80eca..129ca1b892031a4352849a440b03cee5ee866f43 100644
--- a/src/terminal.css
+++ b/src/terminal.css
@@ -46,6 +46,8 @@
 
 .term-grid {
   display: block;
+  min-height: 100%;
+  background: var(--term-bg);
   white-space: pre;
   contain: layout paint style;
   will-change: contents;
@@ -68,6 +70,7 @@
 .term-link > span {
   display: inline-block;
   height: var(--term-row-height);
+  overflow: hidden;
   vertical-align: top;
 }
 
@@ -88,12 +91,12 @@
 }
 
 .term-block {
-  width: 1ch;
+  width: var(--term-cell-width, 1ch);
   overflow: hidden;
 }
 
 .term-wide {
-  width: 2ch;
+  width: calc(2 * var(--term-cell-width, 1ch));
   overflow: hidden;
 }
 
