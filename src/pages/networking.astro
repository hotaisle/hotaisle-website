---
import Page, { generateMetadata } from '@/components/pages/NetworkingPage.tsx';
import BaseLayout from '@/layouts/BaseLayout.astro';

const metadata = generateMetadata();
---

<BaseLayout metadata={metadata}>
	<Page />
</BaseLayout>
